name: weft_fintech
description: Weft FinTech Flutter connectors — WeftOrderBookWidget (CustomPainter depth ladder over the MDP1 snapshot wire), zero-GC frame path, edge-triggered repaints. 240 FPS capable, 0 rebuild storms, book state NEVER crosses into widget state.
version: 0.1.0
homepage: https://github.com/zephyr4289/Weft
repository: https://github.com/zephyr4289/Weft

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0

# NOTE: test/mdp1_logic_test.dart is a dependency-free pure-Dart harness
# (dart run) so the MDP1 decode, CRC and corruption matrix verify on any
# Dart SDK without a Flutter toolchain. Structural audit runs under Node
# (test/static_audit.mjs) — compile lane ships on flutter CI.
