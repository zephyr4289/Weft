name: weft_flutter_example
description: Demonstrates how to use the weft_flutter package on Linux Desktop.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  ffi: ^2.1.0
  flutter:
    sdk: flutter
  weft_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
